name: bridge_native
description: Neos
version: 1.0.0

flutter:
  plugin:
    platforms:
      android:
        package: vn.sun.bridge
        pluginClass: Pigeon
      ios:
        pluginClass: FLTPigeon

dependencies:
  flutter:
    sdk: flutter
  pigeon: ^22.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  injectable_generator: any
  build_runner: ^2.4.15
  built_value_generator: any

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.12.13+hotfix.5"
