include: package:lints/recommended.yaml

analyzer:
  exclude:
    - lib/pages/app_pages.router.dart

formatter:
  page_width: 120

linter:
  rules:
    - prefer_single_quotes
    - avoid_print
    - require_trailing_commas
    - always_specify_types: false